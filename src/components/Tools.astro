---
// Small dedicated tools section on /approach, per the real-content pass — grounded
// directly in xheia-content-worksheet.md's confirmed stack, nothing invented. Copy V2
// pass condensed the 11 individual tools into grouped tags (still the same real
// stack, just fewer/grouped labels).
//
// Rebuilt from a flat pixel-tag list into a real "stack manifest" (Apple HIG design-
// improvement pass — the user directly called out that incremental CSS nudges here
// weren't a real transformation): each tool is its own numbered row, mono-spaced
// like a terminal `ls` listing, tying into the same numbering motif as /approach's
// section numerals instead of just being five identical pills. Deliberately no
// SectionLoader/typed reveal — a flat list of short labels doesn't carry enough
// meaning per item to justify a "boot check," unlike Capabilities/PillarBlock's named
// modules; the whole manifest still fades in as a group on scroll (.section-content).
//
// The section itself is now the one frame (PillarBlock's exact recipe) instead of a
// separate inner `.tools-frame` box, and rows are separated by whitespace, not
// per-row dividers - matching Principles' smoothness, direct feedback that every
// /approach section should read as the same "family."
import SectionNumber from './SectionNumber.astro';

const tools = [
  'Microsoft data & AI stack (Azure, Power BI, Foundry)',
  'Python',
  'SQL',
  'Power Automate',
  'Claude Code',
];
---

<section class="tools" data-reveal>
  <div class="section-head">
    <SectionNumber number="04" />
    <h2 class="eyebrow">// tools</h2>
  </div>
  <p class="frame-label">stack manifest</p>
  <ol class="manifest section-content">
    {
      tools.map((tool, i) => (
        <li class="manifest-row">
          <span class="row-index" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
          <span class="row-name">{tool}</span>
        </li>
      ))
    }
  </ol>
</section>

<style>
  /* No border/tinted background - see Capabilities.astro's equivalent comment: Home's
     whitespace-only framing overrides the earlier "one frame per section" standard. */
  .tools {
    width: min(100% - 2 * var(--space-4), var(--content-max-width));
    margin: var(--space-20) auto;
    padding: 0;
  }

  .section-head {
    display: flex;
    align-items: baseline;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
  }

  /* --accent-primary-text, not --accent-primary: see Capabilities.astro's equivalent
     comment (full audit finding, raw --accent-primary is 4.22:1 against Ink in dark
     mode, below the 4.5:1 text minimum). */
  .eyebrow {
    margin: 0;
    font-family: var(--font-mono);
    font-weight: 400;
    font-size: 0.875rem;
    line-height: 1.5;
    letter-spacing: 0.06em;
    color: var(--accent-primary-text);
  }

  /* typography.md's macOS translation: 13pt default / 10pt minimum (~17px/~13px).
     This was 0.75rem (12px), under that minimum - same fix as Footer's credit
     line and Capabilities' equivalent label. */
  .frame-label {
    margin: 0 0 var(--space-6);
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-secondary);
  }

  .manifest {
    margin: 0;
    padding: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .manifest-row {
    display: flex;
    align-items: baseline;
    gap: var(--space-6);
  }

  /* --accent-primary-text, not --accent-primary: this is real visible text (18px,
     regular weight - under WCAG's large-text threshold) even though aria-hidden.
     Full audit finding, same as the eyebrow labels above. */
  .row-index {
    flex-shrink: 0;
    font-family: var(--font-display);
    font-weight: 400;
    font-size: 1.125rem;
    color: var(--accent-primary-text);
  }

  .row-name {
    font-family: var(--font-mono);
    font-size: 1.1875rem;
    font-weight: 500;
    color: var(--text);
  }

  @media (max-width: 40rem) {
    .manifest-row {
      gap: var(--space-4);
    }

    .row-name {
      font-size: 1.0625rem;
    }
  }
</style>
