---
// One-line identity hook on Home — surfaces who's behind Xheia without turning Home
// into a personal profile (that's /about's job, still the "small space" per round-1
// feedback). Same fact as /about's opening line, grounded in BRANDFINAL §1's origin
// story. Name is a placeholder — see SPEC.md's revision note.
---

<p class="identity-hook" data-reveal>
  <span class="name">[Your Name]</span> — biomedical science background, not the usual
  data path.
  <a href="/about">More about me →</a>
</p>

<style>
  .identity-hook {
    max-width: var(--content-max-width);
    margin: 0 auto;
    padding: 0 var(--space-4) var(--space-8);
    text-align: center;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .name {
    color: var(--text);
    font-weight: 500;
  }

  .identity-hook a {
    margin-left: var(--space-2);
    color: var(--accent-primary);
  }
</style>
