---
// 2-3 sentence excerpt of the origin story + link to /about (SPEC.md — AboutTeaser).
// Condensed directly from BRANDFINAL.md §1 Origin story — no new claims invented.
---

<section class="about-teaser">
  <p>
    A biomedical science background, not the usual data path — an unexpected,
    self-directed move into data work. Four-plus years delivering enterprise-scale
    data consolidation, predictive customer intelligence, and BI across life sciences,
    public sector, retail, education, and fitness.
  </p>
  <a href="/about" class="read-more">Read the full story →</a>
</section>

<style>
  .about-teaser {
    max-width: var(--content-max-width);
    margin: 0 auto;
    padding: var(--space-8) var(--space-4);
  }

  .about-teaser p {
    max-width: 40rem;
    margin: 0 0 var(--space-4);
    color: var(--text-secondary);
  }

  .read-more {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--accent-primary);
  }
</style>
